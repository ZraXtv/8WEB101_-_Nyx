# Configuration locale : contient l'URL et la clé de TON projet Supabase.
# Elle est « publiable » et donc faite pour vivre dans le navigateur, mais
# elle n'a rien à faire dans un dépôt public.
config.js
